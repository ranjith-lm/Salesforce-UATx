<!-- 	
    * 	Created By: Imane Tsioucha
    *	Created Date: 29-11-2023
    * 	Change History: 
-->
<aura:component implements="forceCommunity:availableForAllPageTypes,force:lightningQuickActionWithoutHeader,force:hasRecordId" access="global" controller="LTNG035_CollectionLegalActionController"   >
    
    
    <aura:attribute name="recordTypeId" type="String" default="" />
    <aura:attribute name="showSpinner" type="Boolean" default="false"  />
    <aura:attribute name="caseModel" type="String" />
    <aura:attribute name="recordId" type="Id" />
    <aura:attribute name="Collection_Case__c" type="Boolean" />
    <aura:attribute name="showError" type="Boolean" />


    
    <aura:handler name="init" value="{!this}" action="{!c.doInit}" />

    <header class="slds-modal__header">
        <h2 id="modal-heading-01" class="slds-text-heading_medium slds-hyphenate">
            <span>
                <h4 class="title slds-text-heading--medium">New Collection &amp; Legal Action FCR</h4>
            </span>
        </h2>
    </header>

    <div class="slds-hidden" style="height: 0px;">
        <lightning:recordEditForm recordId="{!v.recordId}" objectApiName="Account" onload="{!c.handleLoad}">
            <lightning:inputField aura:id="Subscription_Model" fieldName="Subscription_Model__pc" />
        </lightning:recordEditForm>
    </div>

    <lightning:card>
        <aura:if isTrue="{! v.recordTypeId != '' }">
            <lightning:recordEditForm aura:id="form"
                                      recordTypeId="{!v.recordTypeId}"
                                      objectApiName="Case"     
                                      onload="{!c.handleOnload}"
                                      onsuccess="{!c.handleOnSuccess}"
                                      onerror="{!c.handleOnError}"
                                      >
                <lightning:messages/>
                <!--on load handle name on Card, handle Submit -->
                <lightning:inputField fieldName="Subject" aura:id="subject"  value="New Collection &amp; Legal Action FCR"/>
                <lightning:inputField fieldName="Description" aura:id="description" value="New Collection &amp; Legal Action FCR" />
                <lightning:inputField fieldName="Case_Model__c" aura:id="caseModel" required="true" value = "{!v.caseModel}"/>

                <lightning:inputField fieldName="RecordType_Name__c" aura:id="CaseRecordTypeName" required="true" value="Collection Legal Action FCR" />
                <lightning:inputField fieldName="Case_Nature__c" aura:id="caseNature" required="true"/>
                <lightning:inputField fieldName="Type" aura:id="type" required="true"  />
                <lightning:inputField fieldName="Sub_Type__c" aura:id="subType" required="true" />
                
                <lightning:inputField fieldName="Origin" aura:id="origin" required="true" />
                
                
                
                <div class="slds-hide">
                    <lightning:inputField fieldName="AccountId" value="{!v.recordId}" aura:id="accId"/>
                </div>
                
                <footer>
                    <div class="slds-float_right">
                        <lightning:button variant="brand" label="Save" disabled="{!v.showSpinner}" onclick="{!c.onSubmit}" />
                        <lightning:button variant="brand" label="Cancel" onclick="{!c.onCancel}" />
                    </div>
                </footer>
            </lightning:recordEditForm>
        </aura:if>
        <aura:if isTrue="{!v.showSpinner}">
            <lightning:spinner aura:id="mySpinner" alternativeText="Saving..." title="Saving.." variant="brand" size="large"/>
        </aura:if>
    </lightning:card>
</aura:component>