<aura:component controller="DeviceDetailsController" implements="lightning:availableForFlowScreens" access="global">
	<aura:attribute name="customerId" type="String"/>
    <aura:attribute name="cifNumber" type="String" access="global"/>
    <aura:attribute name="fdColumns" type="List"/>
    <aura:attribute name="fixedDeposits" type="List"/>
    <aura:attribute name="isLoadingTermDeposits" type="Boolean" default="false"/>
    <aura:attribute name="message" type="String" default = ""/>
    
    <aura:handler name="init" value="{!this}" action="{!c.doInit}"/>
    
    <aura:if isTrue="{!v.isLoadingTermDeposits}">
        <lightning:spinner size="medium"/>
    </aura:if>
    <aura:if isTrue="{!empty(v.fixedDeposits)}">
        <div class="slds-text-color_error slds-text-title_bold">
        	{!v.message}
        </div>
        
        <aura:set attribute="else">
            <lightning:datatable
                                 keyField="id"
                                 data="{!v.fixedDeposits}"
                                 columns="{!v.fdColumns}"
                                 hideCheckboxColumn="true"/>
        </aura:set>
    </aura:if>
    <br/>
</aura:component>