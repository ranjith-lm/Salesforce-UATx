<aura:component controller="LTNG004_AccountClosureController"
    implements="forceCommunity:availableForAllPageTypes,force:lightningQuickActionWithoutHeader,force:hasRecordId" access="global">

    <header class="slds-modal__header">
        <h2 id="modal-heading-01" class="slds-text-heading_medium slds-hyphenate">
            <span>
                <h4 class="title slds-text-heading--medium">Checker Result</h4>
            </span>
        </h2>
    </header>

    <lightning:card>
        <lightning:recordEditForm aura:id="form" recordId="{!v.recordId}" objectApiName="Case" onsubmit="{!c.handleOnSubmit}">
            <lightning:messages />
            <div class="slds-hide">
                <lightning:inputField fieldName="Type"/>
            </div>
            <lightning:inputField fieldName="cc_Checker_Result__c" aura:id="cc_Checker_Result"/>
            <lightning:inputField fieldName="cc_Checker_Comments__c" aura:id="cc_Checker_Comments" />
            
            <footer>
                <div class="slds-float_right">
                    <lightning:button variant="brand" label="Save" type="submit" />
                    <lightning:button variant="brand" label="Cancel" onclick="{!c.onCancel}" />
                </div>
            </footer>
        </lightning:recordEditForm>

    </lightning:card>
    <lightning:spinner aura:id="mySpinner" alternativeText="Saving..." title="Saving.." variant="brand" size="large"
        class="slds-hide" />

</aura:component>